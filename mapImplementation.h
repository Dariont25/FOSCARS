#include <map>
#include <MovieManager.h>

class movieMap {
    Movie movie;
    map <movie, vector<string> movieInfo> movieMap;
public:
    void addMovie() {

    }
    movie returnMovie(int key) {
        if()
    }
};
